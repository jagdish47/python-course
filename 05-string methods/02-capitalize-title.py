#str.capitalize() -> first caracter will be upper case of only first word

phrase = "how are you"
cap_phrase = phrase.capitalize()
print(cap_phrase)

#str.title() -> make capital first letter of each word
all_cap_phase = phrase.title()
print(all_cap_phase)